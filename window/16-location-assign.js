function changeLocation() {
  window.location.assign("https://www.google.com")
}

/**
 * window.location.assign() method loads a new document.
 * It replaces the current document with a new one.
 * window.location.assign() is the same as setting the href property of the location object.
 **/