let scrollX = window.scrollX;
let scrollY = window.scrollY;

console.log(`scrollX: ${scrollX}, scrollY: ${scrollY}`);

/**
 * scrollX: 0, scrollY: 0 Thats because the page is not scrolled.
 * 
 * When you scroll the page and run the code again, you will see the updated values.
 * This will be useful when you want to know the current scroll position of the page.
 **/