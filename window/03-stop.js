function stopLoading() {
  console.log('stop loading');
  // stop loading
  window.stop();
}

/**
 * window.stop() method is used to stop the loading of the current page.
 * 
 * This method is used to stop the loading of the current page. It is equivalent to pressing the stop button in the browser.
 */